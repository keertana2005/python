def differenceSum(n):
    n_str = str(n)
    sum_even = 0
    sum_odd = 0
    
    for i, digit in enumerate(n_str):
        if i % 2 == 0:
            sum_even += int(digit)
        else:
            sum_odd += int(digit)
    
    return abs(sum_even - sum_odd)
